# Database
POSTGRES_USER=blueprint
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=blueprint_db

# Auth
JWT_SECRET=CHANGE_ME        # generate with: openssl rand -hex 32

# Email provider: mailpit (local testing) | gmail (live demo)
EMAIL_PROVIDER=mailpit

# Gmail — only needed when EMAIL_PROVIDER=gmail
GMAIL_USER=
GMAIL_APP_PASSWORD=         # Google App Password (not your account password)
EMAIL_FROM=BlueprintT <your@gmail.com>
