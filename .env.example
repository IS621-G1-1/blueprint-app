# Engineer-facing env. Copy to .env and fill in CHANGE_ME values.
# Never commit .env (gitignored).

# --- Postgres (shared by app DB and Keycloak DB; one instance, two databases) ---
POSTGRES_USER=blueprint
POSTGRES_PASSWORD=CHANGE_ME

# --- Keycloak admin bootstrap (only used on first start) ---
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME

# --- Email provider switch ---
# mailpit (default, local)  → Mailpit at http://localhost:8025; no external account needed
# sendgrid                  → SendGrid; requires SENDGRID_API_KEY + SENDGRID_FROM_EMAIL
EMAIL_PROVIDER=mailpit

# Required only when EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# Used by Mailpit only (purely cosmetic — Mailpit accepts any sender)
SMTP_FROM_EMAIL=noreply@blueprint.local

# --- Backend client secret (Keycloak confidential client) ---
# Used by the backend to: validate JWTs, broker password grant for /auth/login,
# and call the admin API via service account. Generate one with: openssl rand -hex 32
BACKEND_CLIENT_SECRET=CHANGE_ME
